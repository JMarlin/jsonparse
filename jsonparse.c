#include "jsonparse.h"

void JSONParse_parse(char* jsonString) {

	for(char* characterPointer = jsonString; *characterPointer != 0; characterPointer++) {

		char characterValue = *characterPointer;
	}
}
